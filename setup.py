from setuptools import find_packages, setup

setup(
    name="neural_processes",
    packages=find_packages(),
    version="0.0.a",
    description="Implementations & explorations of neural processes",
    author="datavaluepeople",
    license="",
)
